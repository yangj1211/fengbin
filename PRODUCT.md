# 丰宾电子智能制造平台

## Register

product

## Users

丰宾电子销售、设备、能源、生产及采购人员，使用账号名登录企业内部应用。平台预置唯一超级管理员，其他账号分为管理员和普通用户。

## Product Purpose

用户已明确：先完成应用前端和完整操作流程。交付有登录、首页、五个业务模块和用户管理的完整应用界面。登录后直接进入智能体广场，五个智能体各自使用一张卡片入口和独立业务页面；用户可发起问答、查看结果、继续历史对话，并导出明细列表。

## Brand Personality

克制、清晰、专业。延续白色工作区、石墨导航、铜橙主色。

## Design Principles

- 智能体广场支持按名称、用途与业务分类搜索，输入即时筛选卡片，多关键词共同匹配；清空恢复全部，无结果提供清空入口。

- 客户、维修智能体默认进入问答；能源、生产、供应商默认进入数据看板。从智能体广场或左侧导航进入均遵循这一默认顺序。看板标题右侧提供“智能问答”入口，打开该智能体的问答页面并恢复已有对话；没有对话时准备空白对话，不自动发送问题。问答中的“查看看板”及移动端入口继续可用，往返保留草稿与筛选。显式问答地址刷新后仍为问答，不受旧的视图偏好影响。看板支持筛选和图表查看，默认规则直接展示且不可修改，导出仅针对明细列表。
- 问题、条件与结果逐轮保留，精确参数采用可展开面板。
- 正式应用文案，不使用 POC、Demo、运行演示等提示。
- 用户要求前端按正式业务应用呈现，不显示样例、示例、演示等标签。客户产品资料采用客户提供的规格书，其他模块保留现有数据；不假称已接入真实 AI 或客户业务系统；数据覆盖范围、计算规则和缺失信息按业务口径说明。
- 本阶段独立会话、草稿、反馈和看板偏好保存在当前浏览器并按账号隔离，业务数据在该浏览器内共用，历史版本保存的来源删除标记继续生效。工作区先确认登录账号再加载对应记录；旧版未区分账号的对话迁入超级管理员。最左侧显示当前角色可访问的应用目录；聊天区内侧浅色栏用于新建和切换历史会话，左下角显示当前用户姓名，点击后的菜单提供修改密码和退出登录。
- 操作能闭环：发起问答、查看结果、恢复历史对话和导出明细。
- 五个智能体的回答均分为“思考过程”和“最终答案”两阶段逐步显示。“思考过程”使用可公开的简短处理摘要，初始展开，开始输出最终答案时自动折叠，用户可再次展开。当前由前端分段呈现本地回答，尚未接入模型流式接口。停止生成只保留已显示的内容；复制回答包含最终答案和文末引用，不包含处理摘要。旧对话没有摘要时明确说明未记录，不补造过程。

## Accessibility & Inclusion

高对比正文、键盘可用控件、可恢复错误状态、窄屏结构适配和减少动态效果偏好。

## 登录与用户管理

未登录访问应用页面先到登录页，已登录访问登录页回到智能体广场。用户使用账号名和密码登录。登录页不提示用户角色，只有账号、密码和登录按钮，密码支持显示/隐藏；客户端准备好后才允许提交，登录失败保留输入并提示原因。

退出、账号删除、密码或角色修改后，相关登录状态失效。应用切换目录、窗口重新获得焦点及定时检查登录状态，失效后回到登录页。管理员修改自己的姓名后，左下角显示同步更新。

普通用户仅可访问智能体广场和五个智能体，保留问答引用阅读、业务看板及明细导出；导航隐藏整个“平台管理”分组，直接访问用户管理也会拦截。管理员另可使用平台管理中的用户管理。

用户列表显示姓名、账号、角色、创建时间、最近登录和操作；时间统一北京时间，按创建时间倒序排列。姓名/账号搜索即时筛选，清空恢复全部，分页默认10条、每页可选10至100条。筛选及修改每页条数回到第一页，删除当前页最后一条后回到有效页码。

管理员通过弹窗新增或编辑用户：姓名选填、不填时使用账号名，最多40个字符；账号只在新增时填写，创建后不可修改；账号名唯一，保存时去首尾空格并转小写，支持小写英文、数字、点、下划线和短横线，长度3至32个字符；初始密码为8至64个字符且两次输入一致。角色可选管理员或普通用户，新增默认普通用户；超级管理员为唯一固定账号，不可新增、降级或删除。当前登录账号不可删除；管理员修改自己的密码必须验证旧密码；编辑其他用户时为重置密码，无需对方旧密码。保存时防重复提交，失败保留输入并允许重试；新增成功清空搜索并返回第一页，编辑成功保留搜索条件。删除需二次确认。

所有用户均可从左下角账户菜单打开“修改密码”，填写旧密码、新密码和确认新密码。每次打开均为空，旧密码必填，新密码为8至64个字符且两次输入一致；旧密码错误时保留输入并显示错误，修改成功后回到登录页。原型中账号用于演示登录、角色菜单和用户管理规则；会话和业务数据的浏览器存储范围按上述规则区分。

## 客户与产品推荐场景

依据《卓越级智能工厂场景实例描述》的第一个场景，客户问答使用本次提供的 15 个真实型号、15 份原始 PDF 及 50 页原文预览。欢迎页保留三个入口：按参数找型号、比较两款型号、核对纹波电流；共六组问题覆盖参数筛选、耐久条件、替代比较、纹波、寿命类型和尺寸公差。新回答引用真实规格书及页码，旧对话的历史引用继续可读。

仅按明确条件筛选，容量精确匹配、电压为最低额定值、温度须落在工作范围内、安装方式匹配；Useful Life 与 Endurance 分开，纹波保留频率与温度，尺寸采用本体公差上限。缺少测试条件先补问。没有匹配时说明当前 15 份资料内的冲突，不推断其他产品不存在；不从规格书推断报价、库存、交期或实际设备寿命。追问保留条件，点击预设问题开始该组需求。旧草稿确认后才用于新查询。当前仍为前端参数提取与规则查询，尚未接入模型、NL2SQL 或业务数据库。

客户场景回答使用普通文本段落，不显示条件标签、推荐卡片、参数表格、采用动作和快捷追问；逐项对照用户条件与产品参数，所有引用统一放在回答末尾，点击可定位原文件页。


## 设备维修助手场景

第二场景以维修手册、SOP、点检标准、故障码、历史工单、备件与FAQ为依据，支持故障描述、补问、具体维修方案和后续反馈。方案说明异常判断、对应处理动作、备件、注意事项及修后验证。完整需求见 [设备维修助手需求](docs/设备维修助手需求.md)。当前提供五类合成故障和三份示例PDF，欢迎页保持三个入口；不替用户填默认设备或故障。

维修回答统一用通用Markdown渲染，内容按问题组织，引用集中在文末。保留旧对话文本，不显示维修指标、方案卡片、固定步骤组件、采用动作或快捷追问。当前反馈仅随浏览器对话保存，不假称已写入工单。其他三个场景的业务看板保持原有形式。


## 能源场景简化


第三场景通过84条合成明细展示每日能耗趋势、班次/产线对比、异常点列表和基础预测。明细与原工序汇总一致，计算和边界见 [能源场景需求](docs/能源场景需求.md)。日期、工序与产线统一筛选，趋势支持日/月/年汇总；预测天数和计划总产量在预测面板内调整，未填计划时按历史产量延续并明确说明来源。问答与看板共用业务数据，各自按当前查询或筛选条件计算；本地汇总数据不伪造时序。

## 生产洞察与预警

看板底部直接查看产线明细与异常说明，去掉明细切换、逐行操作和节拍/WIP/班次的无数据占位入口。

第四场景保留实际产量、计划完成率、检验良率、累计停机、异常产线五项核心指标。问答支持产量与进度、检验质量、停机情况、产线对比、异常提醒与规则解释五类场景，并可汇总为生产日报。完整口径及示例见 [生产场景需求](docs/生产场景需求.md)。欢迎页三个入口为生成生产日报、比较产线表现、检查综合异常；完整五类示例与日报提问方式可通过“你能做什么”查看。日报沿用当前产线范围，以流式普通文本说明产量、质量、停机、异常和跟进建议，来源放在文末。当前数据没有日期，日报明确统计日期待确认，不认定为今天或指定日期的结果；不提供报告导出。

生产回答使用通用Markdown文本，来源集中在文末，保留点赞、点踩和复制。去掉回答中的条件标签、指标卡片、图表、明细切换及保存分析按钮；旧分析内容按当时结果转为文字，不修改历史数据。当前四条产线汇总没有日期/班次/设备事件维度，不虚构趋势或异常原因。

## 供应商绩效评估

第五场景展示供应商评分、交付风险、质量风险和异常明细；问答覆盖评分排名、交付风险、质量风险、供应商对比、评分与规则解释五类，欢迎页保留三个示例卡片。评分、风险和问答共用计算口径，参考数据集中在普通文本回复末尾。明细支持是否异常筛选，正常行不填写异常说明，不增加逐行操作入口。详见[供应商场景需求](docs/供应商场景需求.md)。

## 回答反馈

每条用户问题也提供独立复制入口，复制其完整原文，历史问题与等待回答中的问题均可使用。

各智能体的每条回答末尾提供点赞、点踩和复制。点赞与点踩互斥，可再次点击取消，评价仅随当前浏览器的会话保存；复制包含回答正文和文末引用，不包含用户提问、智能体名称及操作控件。

## 明细导出与模块范围

分析记录模块已移除，包含导航、广场入口、保存入口和旧路由。各智能体的对话历史仍按当前会话机制保留。

导出统一为真正的 Excel 工作簿（.xlsx），入口位于列表区域；列名、顺序、数值和异常说明与列表共用同一份数据。生产与供应商按当前范围和是否异常筛选导出，能源按当前日期、工序、产线导出全部匹配异常明细，分页不限制导出条数。工作簿使用单个“明细”工作表，冻结表头并启用筛选，保留文本与数字类型。导出期间防止重复点击，失败可重试。空结果禁用导出，不提供报告、图表或对话导出。

## 来源资料

客户与维修保留PDF业务资料入口，能源、生产、供应商保留回答文末的CSV来源预览。CSV文件、预览、看板和问答使用同一组记录；用 `node tools/build-tabular-files.mjs` 从默认业务表重新生成CSV资产，本地自带文件名的数据不指向默认文件。

旧PDF章节ID与页码保留，旧回答仅规范展示文案，不修改历史提问、业务参数或计算结果。现有浏览器中的来源删除标记继续用于历史引用的可用性判断，失效来源不能打开；文件与数据表的历史标识分别处理。应用内不提供资料清单管理、删除或整表导出操作。

## 看板视觉呈现

能源、生产和供应商看板使用统一浅蓝灰背景、深蓝文字，指标、图表和明细直接排在同一画布上，保留现有图表配色、筛选、问答和明细导出。生产新增当前产线停机时长分布；供应商风险分布按每家供应商互斥归类。图表直接复用已有数据与规则，不改变原指标口径，不生成不存在的历史趋势。

看板复用 Tremor 开源组件，图表渲染与业务计算分离；数值、规则、筛选和导出继续使用现有数据模型。

所有业务判定使用固定默认规则，不提供规则配置入口，也不通过对话更改阈值或权重。生产使用完成率95%与不良率2%；供应商目标95%/98%、权重40%/40%/20%。旧会话继续使用时恢复默认规则，历史回答保留原结果。看板的专属配色只在看板显示时生效，问答保持原有浅色背景。

## 多数据展示

图表按完整筛选数据统计，产线与供应商图表每组最多6项，趋势每组最多31个时间点；可前后翻组，不截断后续数据。纵轴或排名条形刻度按完整筛选数据计算，翻组不改变。长名称缩短轴标签，悬停显示全名。产线、工序、供应商范围支持搜索和多选，所选范围同时用于指标、图表、明细、问答与导出；明细采用10至100条/页的数字分页，改变筛选回到第一页。顶部指标和风险分布按完整筛选范围计算，导出包含全部匹配明细，不受当前页影响。

## 对话名称与来源阅读

历史对话提供修改名称图标，点击后在当前行将原标题替换为输入框并聚焦选中，不弹出改名窗口。名称去除首尾空格后须为1至48个字符；修改后参与搜索并保存到当前浏览器，继续提问或保存草稿不覆盖手动名称。Enter或失焦保存，Esc或纯空白输入取消并恢复原标题，中文输入法确认不触发保存。保存失败在当前行提示并保留输入供重试，桌面与移动端历史列表行为一致。

问答中的文件来源在对话内容区内打开，桌面左右各占一半，聊天与原文独立滚动，无蒙版，也不阻止继续提问。同一时间仅显示一份来源，切换对话关闭预览；窄屏改为上下各半。保留PDF定位与翻页、CSV前100条预览和分页。

看板不再显示底部重复的数据来源与自动重算提示；固定规则、异常定义和回答末尾引用仍在各自位置展示。
