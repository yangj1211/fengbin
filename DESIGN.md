# 丰宾电子智能制造平台设计

用户在明亮办公室和会议室使用电脑处理制造业务。保留铜橙品牌动作色，使用深石墨导航、简洁的品牌展示区与明亮工作面。

## 智能体广场

顶部介绍仅保留标题、用途说明和资料分析流程，不展示英文标识及场景数量宣传语。卡片上方右侧提供名称/用途搜索，输入即筛选并同步数量；多关键词共同匹配，清空恢复全部。无结果直接提示并提供清空入口；窄屏搜索框换到标题下方并占满宽度。

五张独立卡片是用户指定的入口结构。卡片使用成套设计的矢量标识：产品匹配、设备诊断、能源预测、生产洞察与供应商评估。蓝黑图形区以低对比连接线衬托各自的身份图标，正文只保留名称、一句用途和入口，取消参数预览、重复分类与能力标签、卡片记录数量。数据通过数据管理访问，对话通过各智能体的历史列表恢复。图形表达身份，不表示实时状态或实测数据；仅入口箭头和连接线在悬停时给予轻量反馈。

卡片采用紧凑尺寸：桌面图形区 120px、卡片最小高度 260px，三列区域最大宽度 1200px，列距 32px、行距 36px；手机保留 24px 卡片间距。五个入口统一显示“开始使用”。

## 业务看板

客户、维修默认进入问答页；能源、生产、供应商默认进入数据看板，广场和左侧导航入口保持一致。看板标题右侧提供“智能问答”入口，恢复当前对话或准备空白对话，不自动发送问题。问答内侧仍提供新建、历史列表和“查看看板”。看板以指标和图表为主，保留筛选与图表查看，固定规则直接以文字说明，Excel 导出仅放在明细列表旁。能源展示情景累计预测、工序电量对比和单位电耗；生产展示五项核心指标、计划与实际、质量表现；供应商展示评分和交付质量风险。未提供的数据不展示占位操作入口。

指标、图表与明细共享连续浅蓝灰底，正文使用深蓝色，辅助文字使用较深的蓝灰色。能源用深青绿曲线，生产用蓝色实际柱与灰蓝计划柱，供应商用深金棕评分排行；橙棕标记需关注项。评分与交付/质量比例保留 0–100 尺度，生产和电耗按真实最大值缩放。图表行可点击钻取，柱形、排行与预测线只做短时进入动效，支持减少动态效果，不引入持续动画或额外绘图库。

## 智能体问答

客户推荐和设备维修默认问答。问答页直接呈现欢迎信息、三个业务专属示例问题、底部多行输入框；移除重复的助手标题栏、返回按钮和当前对话标题。中间问答区独立滚动；发送支持 Enter，中文输入法组合期间不发送，Shift + Enter 换行；输入框不显示快捷键说明。可停止分析和保存草稿。最左侧深色导航始终保留智能体广场、五个智能体和数据管理。问答内容区采用内侧浅色对话栏加右侧聊天区的两列布局；内侧提供新建对话与按当前智能体隔离的历史列表，点击可恢复完整会话，删除需要确认。屏幕宽度不超过 1100px 时，内侧栏通过聊天区“对话历史”按钮打开独立抽屉。左下角仅显示用户名，点击后菜单只提供退出登录；导航收起时显示用户名首字，页面不再提供全局顶栏或重复标题栏，目录折叠按钮位于左侧品牌区；收起后保留图标列和展开入口。详细表单收进“分析条件”，资料按钮进入已有数据管理。

五个场景的回复使用普通文本，按问题安排内容，引用集中在文末。保持宽阅读面。问答以独立会话保存在当前浏览器，每段保留最近 20 轮，旧会话和草稿自动迁移；切换会话前保存当前草稿。前端依据本地数据与可解释规则回答，不暗示已连接真实 AI。

## 视觉与响应

采用统一系统字体、16px 正文、14px 常用说明、12px 元信息；标题 20–34px。主色 oklch(.54 .135 55)，石墨导航 oklch(.205 .014 255)，浅灰工作面 oklch(.977 .004 255)。对话不使用装饰性动画；处理状态以文字呈现，逐步输出不使用闪烁光标。桌面三列广场，窄屏两列/单列；输入区始终可达。

对话栏顶部提供新建对话，有业务看板的场景在其下方提供轻量的“查看看板”，不重复显示智能体名称。目录折叠按钮使用透明底色和浅色图标，悬停与键盘聚焦时给出深色反馈。

对话历史支持按标题、草稿问题、历史问答与分析结果文本即时搜索；范围限定当前智能体，桌面与手机共享搜索状态，支持清空和无结果提示，新建成功后恢复完整列表。

## 客户场景的原文件追溯

客户场景欢迎区只显示三张简洁的示例问题卡片：工业电源选型、小型化选型、型号替代。卡片保留短标题、一句说明和点击入口，桌面三列、窄屏单列；点击发送完整案例问题。五类需求的资料、匹配能力与案例验证全部保留，不与欢迎页入口数量绑定。输入区的“业务资料”打开现有五份文件列表。每轮回答末尾统一展示文件名与页码，正文不穿插文件引用。右侧原文抽屉直接显示从原 PDF 渲染的完整页面，打开即定位到引用页。顶部保留文件名、页码选择与前后翻页，阅读区域独立滚动；去掉重复摘录、跳出打开 PDF 和其他章节列表。单页文件禁用翻页，切换页码从页首开始，图片异常时提供原文件后备入口。问答中原文面板占对话内容区右半屏，与聊天独立滚动，无蒙版或焦点锁定；原页按面板宽度显示。窄屏改为上下各半，输入区保持可用。数据管理中的文件预览沿用独立抽屉。

客户回答以普通文本段落呈现，使用简短型号标题和文字参数对照，明确用户要求、产品实际值及是否符合。去掉条件标签、卡片边框、表格、折叠对比、采用与保存分析按钮、输入框上方快捷追问。缺少参数用文字补问，无匹配时以文字说明最接近产品及冲突。来源只在每条回答末尾显示为文字链接；旧存储数据保留兼容。保持既有导航、会话栏、浅色阅读面和铜橙操作色，不改动其他四个智能体的布局。


## 设备维修对话

- 回复正文共用 `message-content.tsx` 的标准Markdown渲染；使用普通段落、列表和文字标题，不将内容映射为维修专属控件。
- 维持对话原有字号和留白，桌面三个简短示例卡片，窄屏纵向排列。
- 维修输入使用对话描述和业务资料入口，不预设设备或故障选择器；追问和反馈也在输入框完成。
- 与客户场景共用原文件抽屉，来源只显示在文末，进入即定位引用页，支持页码选择与翻页。
- 旧维修分析转换为文本继续阅读，缺少引用的历史记录不添加虚构来源。


## 能源页面简化

能源、生产、供应商默认进入看板，客户和维修默认问答。聊天侧栏在新建对话下方保留“查看看板”入口，仅能源、生产、供应商显示；移动端工具栏也提供入口。看板标题右侧的“智能问答”入口打开该智能体的问答页面，恢复当前对话，没有历史时准备空白对话。问答沿用本对话已确认的查询条件。往返时保存草稿并保留看板筛选状态，回答生成中的离开保护继续生效。


指标、趋势、对比和预测排在同一浅蓝灰画布上，以留白和细分隔线组织。宽屏中趋势跨两列，班次和产线位于其下，预测位于右侧；较窄屏幕自动转为两列或单列，卡片间距22px、内部留白24px。趋势与预测图高240px，坐标字号保持14px并按实际宽度生成。顶部统一筛选起止日期、工序及产线；趋势可切换日/月/年汇总。预测面板提供预测天数与计划总产量，常显来源和计算说明。班次/产线使用并列竖柱对比实际单耗和基准，零起点纵轴统一kWh/千只；柱顶显示数值，悬停展示用电量和去重产量，保留统计范围。异常列表保留完整宽度、独立字段与10至100条每页的数字分页；筛选改变后回到第一页。能源回复共用Markdown正文和通用复制、点赞、点踩。

异常列表分页采用参考图的紧凑横排：左侧展示当前记录范围与总数，右侧为无边框翻页箭头、可直接跳转的数字页码及“条/页”下拉选择。当前页以蓝色细边框标识；首末页箭头禁用，较多页码以省略号收起，移动端按组换行。

## 回答反馈

问题复制图标位于气泡右下方，与回答复制保持相同图标、触屏尺寸和成功提示，不在气泡内堆放操作。

回答操作统一放在引用下方，用轻量的点赞、点踩、复制图标，支持键盘焦点、选中状态和复制反馈；不增加独立评价弹窗。

## 生产问答简化

生产看板底部使用单一产线明细表，计划与实际产量分列，是否异常单独显示“是/否”，正常行的异常说明为空，当前阈值在表头说明。表头右侧用一个“是否异常”下拉框筛选全部、异常或正常明细，不使用明细标签页、行内操作按钮和无数据占位面板。

看板保留五项核心指标；对话正文共用通用Markdown，以文字说明进度、质量、停机、产线对比和异常。欢迎页保持三个示例卡片：生成生产日报、比较产线表现、检查综合异常，完整五类示例及日报提问方式放在能力介绍回复中。日报按当前产线范围流式汇总各项指标、异常及跟进建议，当前资料无日期时明确统计日期待确认，来源放在文末。对话不显示指标卡片、条件标签、结果标签页、保存分析、报告导出或快捷追问按钮；输入区不增加资料与条件设置按钮。旧生产分析按原值转为文字，并在文末保留计算依据与资料名。点赞、点踩、复制沿用共用组件。

## 供应商场景简化

看板保持四项概览，在同一浅色图表区呈现综合评分、交付质量对比和供应风险分布，明细与生产使用同一排版：单一表格、是否异常筛选、正常说明为空。去掉逐行分析按钮、明细标签页、重复返回范围入口及无数据占位面板。问答按评分、交付、质量、对比、规则解释组织普通文字，来源集中在文末。欢迎页保持三个卡片，完整能力示例在介绍回复中展示。

## 导出入口

明细列表旁统一使用“导出明细”，按当前筛选导出所有匹配行，分页只影响浏览。下载格式为 .xlsx，单个“明细”工作表共用列表数据映射，保留列顺序、中文表头和数值；导出时按钮显示“正在导出…”并暂时禁用，失败显示可重试提示；不设报告或图表导出。移除独立分析记录模块及其保存弹窗，聊天历史入口保持原位。

## 数据管理列表

统一列表展示文件和数据表，顶部仅保留名称搜索；类型与关联智能体的筛选移到对应表头，以列名和小筛选图标触发，多选时显示已选数量。下拉支持搜索、连续勾选与轻量“清空”，各列内按任一所选项匹配，两列之间同时满足；筛选重置第一页，预览返回时保留筛选和页码。列表去掉内容列，名称搭配已有线性图标，操作列仅显示预览与删除图标，悬停或键盘聚焦时显示对应文字提示，保留可访问名称。PDF 文件复用原文翻页抽屉；CSV 文件以抽屉预览内容，文件与看板共享数据。表格进入预览页，先展示表名和表注释，再展示前100条记录。资源与数据记录分页沿用能源异常列表样式，包括记录范围、数字页码和10至100条/页选择；保留筛选返回位置，空列表显示共0条。删除弹窗说明资料将无法再预览，历史回答与引用名称仍保留。已删除的文件引用使用灰色文字并标注“文件已删除”，不保留可点击链接；资料列表过滤已删除文件，已打开的预览同步显示“文件已删除，无法查看原文”。保存失败继续保留资料与引用的可用状态。保留白色表面和清晰行距，窄屏筛选换行、表格横向滚动；不保留业务标签页、编辑表单或导入与演示控制区。

## 返回导航

应用返回共用轻量箭头样式，默认无边框、透明背景。详情页将返回箭头放在标题左侧，悬停或键盘聚焦时说明返回目标，描述与标题对齐；移除标题上方独占一行的返回按钮。资料库保留“返回资料列表”文字，使用同一箭头、字号和颜色；“查看看板”作为次要入口使用看板图标。各入口保留原来的状态切换、草稿保护和筛选页码，不依赖浏览器历史；窄屏保证触屏尺寸，标题可换行。

筛选下拉菜单默认位于触发框下方并左对齐，间距6px，不再把当前选项覆盖到触发框上。选项区保留4px内边距、36px行高和明确的悬停/键盘高亮，选中项保留勾选标记；边缘空间不足时沿用组件的避让与滚动。


## 业务看板统一画布

能源、生产和供应商使用同一张浅蓝灰底板，指标带使用更明显的浅蓝底色，图表区使用柔和蓝白底，明细区保留清晰的浅色阅读面。深蓝文字与既有图表颜色保证对比度。大区块使用12px圆角，区内图表共享表面，以留白与细分隔线组织，不再逐图堆叠卡片或阴影。顶部紧凑标题与筛选，下面一排核心指标，图表有主次比例，明细全宽接在下方。能源以大趋势图和紧凑预测并列，下排为班次、产线对比；窄屏依次换行。浅色主题同时覆盖表格、输入与下拉弹层，导航及其他页面保持原样。只有已有时间数据绘制趋势，风险环图仍按互斥状态计数。

图表与指标复用 Tremor 官方开源组件源码，统一使用 AreaChart、LineChart、BarChart、BarList、DonutChart、SparkChart、ProgressCircle 和 Card，适配当前 React/Recharts 版本与既有主题变量。保留官方许可与来源记录，见 components/tremor/NOTICE.md。图表关闭入场动画，保留数值提示、已有筛选联动与可访问文本；日期缺失不补造数据，单点与零值仍可查看。

默认规则说明直接排在生产、供应商筛选栏下方，无折叠、无配置按钮。能源异常定义继续直接展示在异常明细上方。只有可见的看板启用专属画布与下拉样式，保留但隐藏的看板不影响问答页主题。

## 业务文案与资料一致性

用户要求按正式业务应用呈现：页面、欢迎问题、回答、引用文件名和原文预览均不显示样例、示例或演示标签。PDF 正文不显示内部章节 ID，保留文件名与页码。客户和维修引用对应8份业务PDF；能源、生产、供应商文末的数据文件链接在当前页面打开CSV预览。固定业务规则与数据缺失说明保持可见。历史回答仅在显示层统一措辞，用户提问、数值和来源标识保持原样。底层仍为前端模拟，不能新增真实接入、库存或认证等未经支持的声明。

## 大量数据与默认入口

能源、生产、供应商的纯智能体路径默认打开看板；客户和维修的纯路径默认问答。`?mode=chat` 显式进入问答并在刷新后保留，`?mode=dashboard` 继续打开支持的看板；旧视图偏好不覆盖默认入口。显式问答页首次不挂载看板，打开看板时再加载图表。往返保留会话、草稿与筛选，离开智能体后卸载看板。

图表容器保持稳定高度：类别图每组6项、趋势每组31点，底部仅在超过一组时显示当前范围和前后箭头。轴标签过长时省略、Tooltip保留全名；纵轴按全部筛选数据统一刻度。生产和供应商明细复用能源列表的数字分页，允许10至100条/页；分页只影响浏览，汇总、风险分布和导出保持全量。产线、供应商及工序下拉支持搜索和多选，勾选后保持展开；单选显示名称，多选显示已选数量，悬停可查看全名。清空恢复全部，搜索不改变统计范围，筛选变化后明细回到第一页。往返问答与看板保留所选范围，汇总和导出使用全部所选对象。

## 对话与来源交互

回答共用两阶段呈现：先逐步显示简短处理摘要，再逐步显示最终答案。“思考过程”使用轻量文字与展开箭头，不加卡片背景；开始输出答案时默认折叠，用户手动展开后在生成完成时保持展开。最终答案仍使用通用 Markdown，引用位于文末，完成后显示点赞、点踩和复制。摘要位于复制区域之外；旧对话未记录摘要时不补造。停止后保留当前已显示文本并保存停止状态，不附加尚未输出的结构化结果或引用；再次提问由用户主动发送。自动跟随输出只在用户位于消息底部时生效，向上阅读时不强制滚动。

多选筛选的已选数量旁只放无边框“清空”文字，悬停出现下划线，键盘聚焦有轮廓提示；点击清空选择与搜索，恢复全部范围。

历史行悬停或键盘聚焦时显示改名和删除图标，触屏常显；点击改名后在原标题位置显示输入框并聚焦全选，日期与轮次保留，当前行编辑期间隐藏改名、删除图标。不使用改名弹窗或额外保存取消按钮。名称最长48字符，Enter或失焦保存，Esc或纯空白输入取消并恢复原标题，中文输入法确认不触发保存。失败时在当前行显示错误并保留编辑；手动名称持久化并参与搜索，后续问答保持该名称。对话删除继续保留原有确认弹窗。

问答来源使用对话区内的共享面板，不遮罩或禁用聊天；切换文件替换当前预览，切换会话关闭预览。复制回答只包含正文和文末引用，不包含面板中的原文内容。PDF翻页和CSV分页各自保留，长表在面板内横向滚动。

看板明细表后不再放数据来源和筛选自动重算的页脚；规则说明与异常定义继续常显。账户区只显示用户名，不附加头像、角色工作区说明或展开箭头，点击仅显示退出登录。
