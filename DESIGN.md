# 丰宾电子智能制造平台设计

用户在明亮办公室和会议室使用电脑处理制造业务。保留铜橙品牌动作色，使用深石墨导航、简洁的品牌展示区与明亮工作面。

## 智能体广场

五张独立卡片是用户指定的入口结构。卡片使用成套设计的矢量标识：产品匹配、设备诊断、能源预测、生产洞察与供应商评估。蓝黑图形区以低对比连接线衬托各自的身份图标，正文只保留名称、一句用途和入口，取消参数预览、重复分类与能力标签、卡片记录数量。数据与分析记录继续通过应用和记录页访问。图形表达身份，不表示实时状态或实测数据；仅入口箭头和连接线在悬停时给予轻量反馈。

卡片采用紧凑尺寸：桌面图形区 120px、卡片最小高度 260px，三列区域最大宽度 1200px，列距 32px、行距 36px；手机保留 24px 卡片间距。五个入口统一显示“开始使用”。

## 业务看板

能源、生产和供应商首次进入默认业务看板，顶部可切换智能问答；问答页内侧提供新建与历史列表。看板以指标和图表为主，保留筛选、规则调整、钻取、异常分析与 CSV 导出。能源展示情景累计预测、工序电量对比和单位电耗；生产展示五项核心指标、计划与实际、质量表现；供应商展示评分和交付质量风险。缺失的时间、班次、节拍、WIP、价格和订单数据明确标记待补充。

图表核心采用连续深蓝黑分析面板，与明亮的指标条和明细工作面形成主次。底色、悬停与基准线使用同一低饱和蓝色系，浅色文字保持清晰对比。能源用浅青绿预测线及淡面积填充，生产用灰蓝计划柱与浅蓝实际柱，供应商用香槟色评分排行；铜橙标记需关注项。评分与交付/质量比例保留 0–100 尺度，生产和电耗按真实最大值缩放。图表行可点击钻取，柱形、排行与预测线只做短时进入动效，支持减少动态效果，不引入持续动画或额外绘图库。

## 智能体问答

客户推荐和设备维修默认问答。问答页直接呈现欢迎信息、三个业务专属示例问题、底部多行输入框；移除重复的助手标题栏、返回按钮和当前对话标题。中间问答区独立滚动；发送支持 Enter，中文输入法组合期间不发送，Shift + Enter 换行。可停止分析和保存草稿。最左侧深色导航始终保留智能体广场、五个智能体、分析记录和数据管理。问答内容区采用内侧浅色对话栏加右侧聊天区的两列布局；内侧提供新建对话与按当前智能体隔离的历史列表，点击可恢复完整会话，删除需要确认。屏幕宽度不超过 1100px 时，内侧栏通过聊天区“对话历史”按钮打开独立抽屉。左下角头像打开账户菜单并退出登录，页面不再提供全局顶栏或重复标题栏，目录折叠按钮位于左侧品牌区；收起后保留图标列和展开入口。详细表单收进“分析条件”，资料按钮进入已有数据管理。

回复按“采用的条件 → 分析结论 → 明细/依据 → 保存分析”组织，保持宽阅读面。问答以独立会话保存在当前浏览器，每段保留最近 20 轮，旧会话和草稿自动迁移；切换会话前保存当前草稿。前端依据本地数据与可解释规则回答，不暗示已连接真实 AI。

## 视觉与响应

采用统一系统字体、16px 正文、14px 常用说明、12px 元信息；标题 20–34px。主色 oklch(.54 .135 55)，石墨导航 oklch(.205 .014 255)，浅灰工作面 oklch(.977 .004 255)。对话不使用装饰性动画；分析等待使用轻量进度点并支持减少动态效果。桌面三列广场，窄屏两列/单列；输入区始终可达。

对话栏顶部只保留新建对话，不重复显示智能体名称。目录折叠按钮使用透明底色和浅色图标，悬停与键盘聚焦时给出深色反馈。

对话历史支持按标题、草稿问题、历史问答与分析结果文本即时搜索；范围限定当前智能体，桌面与手机共享搜索状态，支持清空和无结果提示，新建成功后恢复完整列表。

## 客户场景的原文件追溯

客户场景欢迎区改为五条紧凑示例需求，输入区的“示例资料”打开现有五份文件列表。每轮回答末尾统一展示文件名与页码，正文不穿插文件引用。右侧原文抽屉直接显示从原 PDF 渲染的完整页面，打开即定位到引用页。顶部保留文件名、页码选择与前后翻页，阅读区域独立滚动；去掉重复摘录、跳出打开 PDF 和其他章节列表。单页文件禁用翻页，切换页码从页首开始，图片异常时提供原文件后备入口。阅读抽屉最大宽度 880px，窄屏原页可横向滚动。

客户回答以普通文本段落呈现，使用简短型号标题和文字参数对照，明确用户要求、产品实际值及是否符合。去掉条件标签、卡片边框、表格、折叠对比、采用与保存分析按钮、输入框上方快捷追问。缺少参数用文字补问，无匹配时以文字说明最接近产品及冲突。来源只在每条回答末尾显示为文字链接；旧存储数据保留兼容。保持既有导航、会话栏、浅色阅读面和铜橙操作色，不改动其他四个智能体的布局。
